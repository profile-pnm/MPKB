// Установка и настройка тайлинга i3wm (на примере Arch)

	== Установка необходимых компонентов ==
1. $ sudo pacman -S xorg-xinit xorg-server i3 i3status (установка оконной системы xorg)
2. $ mkdir vim .xinitrc (в него записать следующее: exec i3)
3. $ sudo pacman -S xf86-video-intel (дрова для видеокарты Intel); xf86-video-nouveau (для NVIDIA); xf86-video-ati (AMD)
	 * Для виртульной машины ( $ sudo pacman -S xf86-video-vesa )
4. $ sudo reboot

	== Layout на i3wm ==
1. sudo setxkbmap -option grp:alt_shift_toggle “us, ru” (ставим русско-английскую раскладку)
2. Переходим в домашний конфиг с i3. Записываем настройку для автозапуска setxkbmap: 
	| #Layoutexec 
	| setxkbmap -option 'grp:alt_shift_toggle'
		или (в том случае если не будет работать)	
	| exec "setxkbmap -model pc104 -layout us,ru -variant ,, -option 'grp:alt_shift_toggle'"
	
	== Конфиг i3 ==
	
# function Win-key #
set $mod Mod4

# polybar #
# exec_always --no-startup-id $HOME/.config/polybar/launch.sh

# Wallapers
exec --no-startup-id feh --bg-fill ~/Pictures/minimalizm.jpg

# Compositor #
exec_always --no-startup-id "killall -q picom; picom --config /etc/xdg/picom.conf" 

# Font for window titles. Will also be used by the bar unless a different font
# is used in the bar {} block below.
font pango: Terminus Bold 15

# us, ru - layout
exec --no-startup-id "setxkbmap -option grp:alt_shift_toggle 'us, ru'"

# Use Mouse+$mod to drag floating windows to their wanted position
floating_modifier $mod

# start a terminal
bindsym $mod+Return exec --no-startup-id alacritty

# kill focused window
bindsym $mod+Shift+q kill

# start rofi (a program launcher)
bindsym $mod+d exec --no-startup-id rofi -theme material -font 'Terminus Bold 20' -show drun -icon-theme "Papirus" -show-icons

# move between windows
bindsym $mod+h focus left
bindsym $mod+j focus down
bindsym $mod+k focus up
bindsym $mod+l focus right

# alternatively, you can use the cursor keys:
bindsym $mod+Left focus left
bindsym $mod+Down focus down
bindsym $mod+Up focus up
bindsym $mod+Right focus right

# move focused window
bindsym $mod+Shift+h move left
bindsym $mod+Shift+j move down
bindsym $mod+Shift+k move up
bindsym $mod+Shift+l move right

# alternatively, you can use the cursor keys:
bindsym $mod+Shift+Left move left
bindsym $mod+Shift+Down move down
bindsym $mod+Shift+Up move up
bindsym $mod+Shift+Right move right

# enter fullscreen mode for the focused container
bindsym $mod+f fullscreen toggle

# change container layout (stacked, tabbed, toggle split)
#bindsym $mod+Shift+s layout stacking
#bindsym $mod+Shift+w layout tabbed
#bindsym $mod+Shift+t layout toggle split

# toggle tiling / floating
bindsym $mod+Shift+space floating toggle

# change focus between tiling / floating windows
bindsym $mod+space focus mode_toggle

	## Autostart apps ##
	exec --no-startup-id sleep 2 && i3-msg alacritty 
	exec --no-startup-id sleep 4 && i3-msg 'workspace 2; exec librewolf; workspace 1'
	exec --no-startup-id flameshot 



	## set floating for apps needing it ##
	
	for_window [class="Pavucontrol" instance="pavucontrol"] floating enable



	## switch to workspace ##
	bindsym $mod+1 workspace 1
	bindsym $mod+2 workspace 2
	bindsym $mod+3 workspace 3



	## move focused container to workspace ##
	bindsym $mod+Shift+1 move container to workspace 1
	bindsym $mod+Shift+2 move container to workspace 2
	bindsym $mod+Shift+3 move container to workspace 3



	## reload the configuration file ##
	
	# bindsym $mod+Shift+c reload



	## fast restart i3-environment ## 
	
	bindsym $mod+Shift+R restart



	## exit i3 and output message ##

	bindsym $mod+Shift+X exec "i3-nagbar -t warning -m '		Ты действительно хочешь выйти из i3?' -b 'ДА!' 'i3-msg exit'"



	## resize window ##
mode "resize" {
        bindsym h resize shrink width 10 px or 10 ppt #RIGHT
        bindsym j resize grow height 10 px or 10 ppt  #UP
        bindsym k resize shrink height 10 px or 10 ppt #DOWN
        bindsym l resize grow width 10 px or 10 ppt #LEFT

        bindsym Left resize shrink width 10 px or 10 ppt
        bindsym Down resize grow height 10 px or 10 ppt
        bindsym Up resize shrink height 10 px or 10 ppt
        bindsym Right resize grow width 10 px or 10 ppt

        # back to normal mode: Enter or Escape
        bindsym Return mode "default"
        bindsym Escape mode "default"
}

	bindsym $mod+r mode "resize"

	 
	
	## i3gaps ##
	 
# Borders for windows
for_window [class=".*"] border pixel 3

# Gaps
gaps inner 10
gaps outer 4

	## Color theme of borders ##
client.focused              #ffffff #ffffff #ffffff #ffffff #ffffff
client.focused_inactive     #727272 #727272 #727272 #727272 #727272
client.unfocused            #727272 #727272 #727272 #727272 #727272
client.urgent               #2f343f #2f343f #d8dee8 #2f343f #2f343f
client.placeholder          #2f343f #2f343f #d8dee8 #2f343f #2f343f
client.background           #2f343f
