// Установка Arch Linux
	== Базовая часть ==
1. cfdisk /dev/sda (создаем два раздела «корень / » и «подкачку swap»)
2. mkswap /dev/sdaN (форматирование в раздел подкачки)
3. swapon /dev/sdaN (форматирование в раздел подкачки)
4. mkfs.ext4 /dev/sdaN(форматирование коренного раздела в файловую систему «ext4»)
5. mount /dev/sdaN /mnt/(монтирование коренного раздела в каталог«/mnt»)
6. pacstrap /mnt base base-devel linux linux-firmware vim(запуск скрипта для установки всех нужных пакетов-		инструментов Arch-а ядро, прошивки известных устройств)
7. genfstab -U /mnt >> /mnt/etc/fstab(генерируем «fstab»)

	== Работа в системе ==
1. arch-chroot /mnt(вход в саму систему, в корень)
2. # vim /etc/locale.gen (расскоментировать строку необходимые строки)
3. # locale-gen(обновить локаль)
4. # vim /etc/locale.conf (в созданный файл записать: LANG=ru_RU.UTF-8) 4 и 5 пункт не обязательно выполнять – можно и в английской локалиостаться. Я так больше предпочитаю. 
5. # /etc/vconsole.conf(записать FONT=cyr-sun16
KEYMAP=ruwin_alt_sh-UTF-8)
6. # pacman -S grub nano networkmanager(устанавливаем загрузчик, редактор и псевдо-графический сетевой менеджер)
7. # grub-install /dev/sdа(установка самого загрузчикана носитель )
8. # grub-mkconfig -o /boot/grub/grub.cfg(автоматическая конфигурация загрузчика)
9. # passwd root(установка пароля на суперпользователя)
10. # useradd -m -g users -G wheel -s /bin/bash «имя пользователя»(добавления обычного пользователя в среду)
11. exit(выход из chroot «из основной системы»)
12. umount -R /mnt(размонтирование всех разделов)
13. reboot(перезапуск системы)

	== Настройка ==
1. # systemctl enable NetworkManager(включение службы «Network Manager»)
2. # systemctl start NetworkManager(запуск службы «Network Manager»)
3. # nmtui(запуск псевдо-графической утилиты для конфигурации сети)
	* Перый пункт можно отредактировать (еслинадо)
	* Второй пункт (активируем соединение)
	* Третий пункт (создаем имя нашего компьютера «хоста» )
4. # ping -c 3 archlinux.org(проверка сети „диагностика сети“)
5. # pacman -S sudo(установка скрита «sudo» для обычного пользователя)
6. # nano /etc/sudoers(расскоментируем « %wheel ALL=(ALL) ALL ») или ниже root ... прописать тоже самое только в начале указать имя пользователя.
7. # passwd «имя пользователя»

PS: Удачи!
